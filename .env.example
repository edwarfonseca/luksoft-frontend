# URL base del servidor API (sin /api al final).
# En desarrollo local, omite esta variable (usa el proxy de Vite).
# En producción (Vercel), ponla como variable de entorno:
#   VITE_API_URL=https://tu-server.railway.app
VITE_API_URL=
